<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>注册 - 街舞网站</title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .container {
            position: relative;
            width: 100%;
            max-width: 400px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #333;
        }
        
        .register-form {
            background: white;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        
        .register-form h2 {
            margin-bottom: 20px;
            color: #333;
            font-size: 20px;
        }
        
        .register-form p {
            margin-bottom: 30px;
            color: #666;
            font-size: 14px;
        }
        
        .success-message {
            background-color: #e8f5e8;
            color: #2e7d32;
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 20px;
            font-size: 14px;
        }
        
        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #666;
            font-size: 14px;
        }
        
        .form-group input {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 14px;
            transition: border-color 0.3s;
        }
        
        .form-group input:focus {
            outline: none;
            border-color: #888;
        }
        
        .password-container {
            position: relative;
        }
        
        .password-toggle {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #888;
        }
        
        .btn-primary {
            width: 100%;
            padding: 12px;
            background-color: #8B4513;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        
        .btn-primary:hover {
            background-color: #6B3410;
        }
        
        .form-links {
            margin-top: 20px;
            font-size: 14px;
        }
        
        .form-links a {
            color: #888;
            text-decoration: none;
            transition: color 0.3s;
        }
        
        .form-links a:hover {
            color: #333;
        }
        
        .error-message {
            background-color: #ffebee;
            color: #c62828;
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 20px;
            font-size: 14px;
        }
        
        @media (max-width: 768px) {
            .login-form {
                padding: 30px 20px;
            }
            
            .container {
                max-width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">IN ONE</div>
        </div>
        
        <div class="register-form">
            <h2>注册新账号</h2>
            <p>填写以下信息创建您的账户</p>
            
            <% if resource.errors.any? %>
            <div class="error-message">
                <%= resource.errors.full_messages.first %>
            </div>
            <% end %>
            
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { id: 'register-form' }) do |f| %>
                <div class="form-group">
                    <label for="<%= resource_name %>_username">用户名</label>
                    <%= f.text_field :username, placeholder: "请输入用户名", class: "form-control" %>
                </div>
                
                <div class="form-group">
                    <label for="<%= resource_name %>_email">邮箱</label>
                    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "请输入邮箱", class: "form-control" %>
                </div>
                
                <div class="form-group">
                    <label for="<%= resource_name %>_password">密码</label>
                    <div class="password-container">
                        <%= f.password_field :password, autocomplete: "new-password", placeholder: "请输入密码（至少8位）", class: "form-control" %>
                        <span class="password-toggle" onclick="togglePassword('<%= resource_name %>_password')">👁️</span>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="<%= resource_name %>_password_confirmation">确认密码</label>
                    <div class="password-container">
                        <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "请再次输入密码", class: "form-control" %>
                        <span class="password-toggle" onclick="togglePassword('<%= resource_name %>_password_confirmation')">👁️</span>
                    </div>
                </div>
                
                <%= f.submit "注册", class: "btn-primary" %>
                
                <div class="form-links">
                    <p>已有账号？ <%= link_to "立即登录", new_session_path(resource_name) %></p>
                </div>
            <% end %>
        </div>
    </div>
    
    <script>
        // 切换密码显示/隐藏
        function togglePassword(fieldId) {
            const passwordInput = document.getElementById(fieldId);
            const passwordToggle = passwordInput.nextElementSibling;
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                passwordToggle.textContent = '👁️‍🗨️';
            } else {
                passwordInput.type = 'password';
                passwordToggle.textContent = '👁️';
            }
        }
    </script>
</body>
</html>
