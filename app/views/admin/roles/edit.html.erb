<% content_for :title, "编辑角色 - #{@role.name}" %>

<div class="card">
  <div class="card-header">
    <h3>编辑角色</h3>
    <%= link_to '返回', admin_role_path(@role), class: 'btn btn-secondary' %>
  </div>
  <%= render 'form', role: @role, permissions: @permissions %>
</div>
